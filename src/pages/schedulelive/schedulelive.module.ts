import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SchedulelivePage } from './schedulelive';

@NgModule({
  declarations: [
    SchedulelivePage,
  ],
  imports: [
    IonicPageModule.forChild(SchedulelivePage),
  ],
})
export class SchedulelivePageModule {}
